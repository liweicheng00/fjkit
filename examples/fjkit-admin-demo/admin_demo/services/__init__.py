"""Business logic, one package per feature.

There is very little of it: an admin's whole point is that the list, the filters
and the form need none. What is here is the rule an action applies, which is the
one thing `fjkit-admin` cannot infer from a model.
"""
