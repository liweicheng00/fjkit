"""Marks the `luma` style pack installed; fjkit reads the entry-point name, not this module.

Against the default `vega`: pill buttons (`rounded-4xl`) and inputs
(`rounded-3xl`) at `h-9`, `rounded-4xl` cards.
"""

PACK = "luma"
