"""Marks the `lyra` style pack installed; fjkit reads the entry-point name, not this module.

Against the default `vega`: `rounded-none` throughout, `h-8` controls,
`text-xs` labels, a 1 px focus ring.
"""

PACK = "lyra"
