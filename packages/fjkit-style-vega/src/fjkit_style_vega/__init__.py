"""Marks the `vega` style pack installed; fjkit reads the entry-point name, not this module.

The default pack, and upstream Basecoat's own: `rounded-md` buttons and
inputs, `h-9` controls, `rounded-xl` cards.
"""

PACK = "vega"
